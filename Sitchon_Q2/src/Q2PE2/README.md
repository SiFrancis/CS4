Using this to test out the **GUI Builder** feature of NetBeans, so I'm unsure if the .java files can be run by themselves.
To be safe, place the .java and .form files in the same package and run them in NetBeans.